{
  "scripts": {
    "prepare-backend": "cd backend && npm install",
    "prepare-frontend": "cd frontend && npm install",
    "build-frontend": "cd frontend && npm run build",
    "start": "node backend/server.js",
    "app": "nodemon backend/server.js",
    "test": "cd frontend && npm test",
    "heroku-postbuild": "npm run prepare-frontend && npm run build-frontend"
  },
  "dependencies": {
    "config": "^3.3.8",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
